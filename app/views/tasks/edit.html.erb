<h1><%= "「#{current_user.name}」のタスク編集" %></h1>

<%= render 'form', task: @task %>

<br>

<%= link_to '詳細', @task, class:"btn btn-outline-info" %> 
<%= link_to '戻る', tasks_path, class:"btn btn-outline-info" %>
